# === General App Info ===
spring.application.name=SpringBoot
server.port=8080

# === LAB 2 ===
app.example.integer-value=9876
app.example.decimal-value=1.75
app.example.boolean-value=true
# === LAB3 ===
server.port=8080
#server.servlet.context-path=/SpringMvcFormExample

# === LAB 11 ===
lab11.api.base-url=/springrest
# lab11.server.port=8080  (Server port is now set once at the top)

# === LAB 12 (JDBC) ===
lab12.api.base-url=/api/books
# lab12.server.port=8080  (Server port is now set once at the top)

# === LAB 13 ===
lab13.api.base-url=/api/user
# lab13.server.port=8080  (Server port is now set once at the top)

# === MySQL Connection ===
# \u2705 Note: All JPA labs will use this database.
spring.datasource.url=jdbc:mysql://localhost:3306/wipro2025
spring.datasource.username=root
spring.datasource.password=Vinay@2025G
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# \u2705 JPA Settings (used by JPA labs like Lab13, Lab14, and Lab15)
# `update` will create the 'client' table in the wipro2025 database for Lab 15.
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect
spring.jpa.show-sql=true

# === JDBC Logging ===
spring.sql.init.mode=always
logging.level.org.springframework.jdbc.core=DEBUG

# === JSP Configuration (used by JSP labs, e.g., Lab 3) ===
# \u2705 Note: You must COMMENT OUT this section when running Lab 15.
# spring.mvc.view.prefix=/WEB-INF/jsp/
# spring.mvc.view.suffix=.jsp

# === Thymeleaf Configuration (used by Thymeleaf labs, e.g., Lab 15) ===
# \u2705 Note: You must UNCOMMENT this section when running Lab 15.
spring.thymeleaf.prefix=classpath:/templates/
spring.thymeleaf.suffix=.html
spring.thymeleaf.cache=false